package com.tsh.slt.agent.domain.product.repository;

import com.tsh.slt.agent.domain.product.model.SltrLcProdRegist;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SltrLcProdRegistRepository extends JpaRepository<SltrLcProdRegist, String> {
}
